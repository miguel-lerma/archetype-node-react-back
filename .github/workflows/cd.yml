name: CD to Azure Container Apps

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

permissions:
  id-token: write     # OIDC para azure/login
  contents: read

jobs:
  deploy:
    name: Deploy to Azure Container Apps
    runs-on: ubuntu-latest
    environment: dev

    env:
      ACA_ENV_NAME: env-lab-aca
      ACR_LOGIN_SERVER: itxzacrlab01.azurecr.io
      ACR_NAME: itxzacrlab01
      APP_ENV: dev
      APP_NAME: be-dev
      AZURE_CLIENT_ID: d986d4dd-0537-4052-9ab0-1e0dbf02e43b
      AZURE_SUBSCRIPTION_ID: a7fb9745-e74d-4e51-b7da-75b90a77b7fd
      AZURE_TENANT_ID: a3907a6d-bbd7-425a-9e46-d06e8a173ba6
      AZ_LOCATION: eastus
      AZ_RESOURCE_GROUP: ITX-RG-01
      CPU: "0.25"
      HTTP_CONCURRENCY: "100"
      IMAGE_NAME: archetype-back
      INGRESS: external
      MAX_REPLICAS: "1"
      MEMORY: 0.5Gi
      MIN_REPLICAS: "0"
      KV_NAME: itxzakvlab01
      # APP_PORT: "3000"   # descomenta si tu contenedor expone un puerto específico

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validar variables requeridas
        shell: bash
        run: |
          set -euo pipefail
          for v in ACA_ENV_NAME ACR_LOGIN_SERVER ACR_NAME APP_ENV APP_NAME AZURE_CLIENT_ID AZURE_SUBSCRIPTION_ID AZURE_TENANT_ID AZ_LOCATION AZ_RESOURCE_GROUP CPU HTTP_CONCURRENCY IMAGE_NAME INGRESS MAX_REPLICAS MEMORY MIN_REPLICAS; do
            if [ -z "${!v:-}" ]; then echo "❌ Falta variable: $v"; exit 1; fi
          done

      - name: Login a Azure (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}
          audience: api://AzureADTokenExchange

      - name: Instalar/actualizar extensión containerapp (az)
        uses: azure/CLI@v2
        with:
          azcliversion: agentazcliversion
          inlineScript: |
            az extension add -n containerapp --upgrade || true

      - name: Verificar ACR existe
        shell: bash
        run: |
          set -euo pipefail
          echo "🔍 Verificando ACR '${ACR_NAME}' en RG '${AZ_RESOURCE_GROUP}'..."
          az acr show -n "${ACR_NAME}" -g "${AZ_RESOURCE_GROUP}" 1>/dev/null

      - name: Calcular tag e imagen
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          TAG="develop-${GITHUB_SHA}"
          IMG="${ACR_LOGIN_SERVER}/${IMAGE_NAME}:${TAG}"
          echo "tag=${TAG}"   >> "$GITHUB_OUTPUT"
          echo "image=${IMG}" >> "$GITHUB_OUTPUT"
          echo "📦 Tag: ${TAG}"
          echo "🖼️  Image: ${IMG}"

      - name: Login en ACR + build & push
        env:
          IMG: ${{ steps.meta.outputs.image }}
        shell: bash
        run: |
          set -euo pipefail
          if az acr login --name "${ACR_NAME}"; then
            echo "✅ Login ACR correcto"
          else
            echo "⚠️  Login ACR standard falló; usando token temporal..."
            TOKEN=$(az acr login --name "${ACR_NAME}" --expose-token --query accessToken -o tsv)
            echo "$TOKEN" | docker login "${ACR_LOGIN_SERVER}" -u 00000000-0000-0000-0000-000000000000 --password-stdin
          fi

          echo "🏗️ Construyendo ${IMG}"
          docker build -t "${IMG}" .

          echo "🚀 Pushing ${IMG}"
          for i in {1..3}; do
            if docker push "${IMG}"; then
              echo "✅ Imagen enviada: ${IMG}"
              exit 0
            fi
            echo "⏳ Reintentando push (${i})..."
            sleep 10
          done
          echo "❌ Falló el push tras 3 intentos"; exit 1

      - name: Asegurar entorno e identidad del Container App (sin tocar imagen)
        shell: bash
        run: |
          set -euo pipefail
          # Crear env si no existe
          az containerapp env show -n "${ACA_ENV_NAME}" -g "${AZ_RESOURCE_GROUP}" >/dev/null 2>&1 || \
          az containerapp env create -n "${ACA_ENV_NAME}" -g "${AZ_RESOURCE_GROUP}" -l "${AZ_LOCATION}"

          # Si el Container App existe, asegurar identidad system-assigned
          if az containerapp show -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" >/dev/null 2>&1; then
            CA_MI=$(az containerapp show -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" --query "identity.principalId" -o tsv)
            if [ -z "${CA_MI}" ] || [ "${CA_MI}" = "None" ]; then
              echo "🆔 Asignando identidad system a ${APP_NAME}..."
              az containerapp identity assign -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" --system-assigned >/dev/null
            fi
          fi

          # Conceder rol AcrPull a la identidad del CA (si existe)
          ACR_ID=$(az acr show -n "${ACR_NAME}" -g "${AZ_RESOURCE_GROUP}" --query id -o tsv)
          CA_MI=$(az containerapp show -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" --query "identity.principalId" -o tsv || echo "")
          if [ -n "${CA_MI}" ] && [ "${CA_MI}" != "None" ]; then
            HAS_ROLE=$(az role assignment list --assignee "${CA_MI}" --role "AcrPull" --scope "${ACR_ID}" --query "length(@)" -o tsv || echo 0)
            if [ "${HAS_ROLE}" = "0" ]; then
              echo "🔐 Asignando rol AcrPull en el ACR..."
              az role assignment create \
                --assignee-object-id "${CA_MI}" \
                --assignee-principal-type ServicePrincipal \
                --role "AcrPull" \
                --scope "${ACR_ID}" >/dev/null
            else
              echo "✅ Rol AcrPull ya asignado."
            fi
          fi

      - name: Mostrar imagen actual en ACA (antes)
        shell: bash
        run: |
          echo "🔎 Imagen actual en ACA (antes del update):"
          az containerapp show -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" \
            --query "properties.template.containers[].{name:name,image:image}" -o table || true

      # 🔥 ÚNICO update: imagen + registry juntos (evita crear revisión con tag viejo)
      - name: Update a la revisión con la imagen recién subida
        env:
          IMG: ${{ steps.meta.outputs.image }}
        shell: bash
        run: |
          set -euo pipefail
          echo "🎯 Imagen objetivo: ${IMG}"
          TAG="${IMG##*:}"
          REPO="${IMAGE_NAME}"
          EXISTS=$(az acr repository show-tags -n "${ACR_NAME}" --repository "${REPO}" --query "[?@=='${TAG}'] | length(@)" -o tsv)
          if [ "${EXISTS}" != "1" ]; then
            echo "❌ El tag ${TAG} no existe en ${ACR_NAME}/${REPO}"; exit 1
          fi

          # Importante: pasamos el registry en el MISMO update para no forzar revisión con imagen vieja
          az containerapp update \
            -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" \
            --image "${IMG}" \
            --container-name "${APP_NAME}" \
            --registry-login-server "${ACR_LOGIN_SERVER}" --registry-identity system \
            --revision-suffix "r-$(date +%s)" \
            --set-env-vars IMAGE_TAG="${TAG}" NODE_ENV="${APP_ENV}" HTTP_CONCURRENCY="${HTTP_CONCURRENCY}"

      - name: Mostrar imagen actual en ACA (después)
        shell: bash
        run: |
          echo "✅ Imagen en ACA tras el update:"
          az containerapp show -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" \
            --query "properties.template.containers[].{name:name,image:image}" -o table

      - name: Mostrar URL pública (si hay Ingress)
        shell: bash
        run: |
          FQDN=$(az containerapp show -n "${APP_NAME}" -g "${AZ_RESOURCE_GROUP}" --query "properties.configuration.ingress.fqdn" -o tsv || true)
          if [ -n "${FQDN}" ] && [ "${FQDN}" != "None" ]; then
            echo "🌐 https://${FQDN}"
          else
            echo "ℹ️ El Container App no tiene ingress público configurado."
          fi